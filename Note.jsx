import React from "react";
function Note() {
  return (
    <div className="note">
      <h1> javascript and react.js</h1>
      <p>
        JSX allows us to write HTML element in JS and place them in the DOM without any createElement() and/or appenedChild() methods.
      </p>
    
    </div>
  );
}
export default Note;
